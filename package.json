{
  "name": "project-4-v0",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "^20.17.0"
  },
  "packageManager": "pnpm@9.10.0",
  "scripts": {
    "clean": "git clean -xdf .sst node_modules && pnpm -r clean",
    "dev": "sst dev",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "sst": "catalog:",
    "typescript": "catalog:"
  }
}
