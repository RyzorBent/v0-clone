{
  "name": "project-4-v0",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20.16.0"
  },
  "packageManager": "pnpm@9.10.0",
  "scripts": {
    "clean": "pnpm -r run clean && git clean -xdf .sst node_modules",
    "dev": "sst dev",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "sst": "catalog:",
    "typescript": "catalog:"
  }
}
