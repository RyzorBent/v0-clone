{
  "name": "project-4-v0",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "^20.17.0"
  },
  "packageManager": "pnpm@9.10.0",
  "scripts": {
    "clean": "git clean -xdf .sst node_modules && pnpm -r clean",
    "dev": "sst dev",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@tsconfig/node18": "^18.2.4",
    "@types/node": "^20.16.5",
    "pulumi": "^0.0.1",
    "sst": "^3.17.0",
    "sst-win32-x64": "^3.17.0",
    "typescript": "catalog:"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
